{
  "name": "daemon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx pm2 start ./src/index.js --watch --no-daemon",
    "del": "pm2 del all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2",
    "node-cron": "^3.0.2",
    "pm2": "^5.3.0"
  }
}
